services:
  - type: web
    name: md-pdf-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3500
      - key: NODE_ENV
        value: production
    plan: free
